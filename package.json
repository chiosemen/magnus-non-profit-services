{
  "name": "magnus-non-profit-services",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm test:integration",
    "test:integration": "vitest run",
    "test:workspace": "pnpm -r --if-present test",
    "prisma:generate": "pnpm --filter @magnus/db prisma:generate",
    "prisma:migrate": "pnpm --filter @magnus/db prisma:migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.29.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma"
    ]
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "@types/supertest": "^6.0.3",
    "dotenv": "^16.6.1",
    "jsonwebtoken": "^9.0.2",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4"
  }
}
