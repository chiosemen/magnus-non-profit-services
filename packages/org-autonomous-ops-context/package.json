{
  "name": "@magnus/org-autonomous-ops-context",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node --test dist/tests/*.test.js",
    "launch-readiness": "pnpm build && node dist/launchReadinessCli.js"
  },
  "dependencies": {
    "@magnus/db": "workspace:*",
    "@magnus/subscription": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
