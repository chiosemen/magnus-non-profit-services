# Release Record — `9030f8b`

- **Audited ref:** `9030f8b` — merge of PR #9 (`codex/staging-pilot-followup`) into `main`
- **Record date:** 2026-08-18
- **Remediation branch:** `claude/magnus-accord-p0-remediation-6p3e33` (Magnus Accord P0 remediation per SPEC-P0)
- **Release status:** `NOT APPROVED FOR PRODUCTION` — this record tracks the evidence needed to change that; it is not itself an approval.

This document supersedes the P0 gate tracking previously held in the root
`BLOCKERS_TO_PRODUCTION.md`. The original document is archived verbatim at
[`docs/releases/archive/BLOCKERS_TO_PRODUCTION-2026-06-11.md`](archive/BLOCKERS_TO_PRODUCTION-2026-06-11.md)
(dated 2026-06-11); the root file now only points here.

Every claim below states its provenance. `VERIFIED` means evidence exists and
is referenced; `PENDING` means the gate is open and no evidence exists yet.
No value in this document is estimated or assumed.

## CI evidence

- Workflow: [`ci.yml`](../../.github/workflows/ci.yml) — TruffleHog secret
  scan, lint, Prisma generate, package builds, `tsc --noEmit`, migration
  apply against an ephemeral PostgreSQL 16 service, full test suite with
  `REQUIRE_DB_TESTS=1`, workspace build, additive-only migration validation,
  Docker build check.
- Workflow runs: <https://github.com/chiosemen/magnus-non-profit-services/actions/workflows/ci.yml>
- Concrete evidence for this remediation: `VERIFIED` — CI and Docker Build
  Check both green on PR #11 head `b3a6e1c`:
  <https://github.com/chiosemen/magnus-non-profit-services/actions/runs/32148056136>
  (2026-08-18, zero skipped tests; packages/db 36 pass, org-autonomous-ops-context
  110 pass, agents 58 ≥ floor 51, mcp-connector 41, web 37 with middleware in
  the built artifact).
- Local pre-push verification (2026-08-18, this branch): workspace
  `tsc --noEmit` exit 0; `apps/web` production build emits middleware
  (`ƒ Middleware` present, manifest test 3/3); migration chain applies
  cleanly on PostgreSQL 16.13 (see Database integration). Tests that require
  Prisma query engines were exercised in CI only, because the authoring
  sandbox cannot download engine binaries.

## Staging smoke

- Latest recorded staging evidence:
  [`docs/operations/P0_PRODUCTION_HARDENING_STAGING_SMOKE_RESULTS.md`](../operations/P0_PRODUCTION_HARDENING_STAGING_SMOKE_RESULTS.md)
  (2026-06-18, environment `magnus-accord-staging`, final verdict
  `PRIVATE PILOT BLOCKED`).
- That evidence predates `9030f8b` remediation work and does not cover the
  P0 fixes on this branch. **Gate: `PENDING`** — rerun the staging smoke per
  [`docs/STAGING_RUNBOOK.md`](../STAGING_RUNBOOK.md) after this branch
  merges and deploys to staging, and link the rerun results here before any
  release approval.
- **Live baseline captured 2026-08-18 against PRE-DEPLOY staging**
  ([run 32173120899](https://github.com/chiosemen/magnus-non-profit-services/actions/runs/32173120899),
  staging still serving the 2026-06-18 build):
  - Health: `staging.magnusnonprofitservices.com/api/health`,
    `accord-org-dashboard-api-staging`, `accord-mcp-connector-staging` all
    returned `{"ok":true}` — `VERIFIED`.
  - Security headers: full live capture — CSP with `frame-ancestors 'none'`,
    HSTS `max-age=63072000; includeSubDomains; preload`,
    `X-Frame-Options: DENY`, `nosniff`,
    `Referrer-Policy: strict-origin-when-cross-origin`, Permissions-Policy —
    all present, `VERIFIED`.
  - `/app` unauthenticated: `307 -> /login?next=%2Fapp`. **This does NOT
    prove the middleware is live.** `apps/web/src/app/(protected)/app/page.tsx`
    calls `requireAuthOrRedirect('/app')`, a server-component guard that
    emits the identical redirect with the middleware absent — as this
    pre-fix run demonstrates. A middleware-specific probe (Check 4,
    a page-less route under the `/app/:path*` matcher) was added because of
    this finding; it must be run against a post-deploy staging.
- Verification mechanism: the `Staging Verification` workflow
  ([`.github/workflows/staging-verify.yml`](../../.github/workflows/staging-verify.yml),
  manual `workflow_dispatch`) runs the release-gate checks (health endpoints,
  live security headers, unauthenticated `/app` → `/login` middleware
  redirect) from GitHub-hosted runners against the deployed staging host.
  Its run URL is the evidence to link here once it passes against the
  post-merge deploy.

## Security header check

- Configured headers (source-verified in
  [`apps/web/next.config.js`](../../apps/web/next.config.js)):
  `Content-Security-Policy` (default-src 'self'; frame-ancestors 'none';
  upgrade-insecure-requests in production), `Strict-Transport-Security`
  (max-age=63072000; includeSubDomains; preload), `X-Frame-Options: DENY`,
  `X-Content-Type-Options: nosniff`, `Referrer-Policy:
  strict-origin-when-cross-origin`, `Permissions-Policy` (restrictive).
- The auth middleware guarding `/app/:path*` is now verified present in the
  **built artifact** by `apps/web/__tests__/middleware-manifest.test.js`
  (P0-6): the compiled `middleware-manifest.json` must register `"/"` with
  matcher `/app/:path*`. At `9030f8b` the manifest was empty — the guard
  did not ship.
- **Gate: `PENDING`** — after the next staging deploy, run the
  `Staging Verification` workflow (Check 2 captures the full live header set
  and fails on any missing header) and link the run here. No header check
  against a deployed environment has been recorded since `9030f8b`.

## Database integration

- Migration chain: 21 migrations (20 pre-existing + 1 additive migration
  `20260818000000_add_s4np_phase3_4_5_missing_tables` added by this
  remediation). `VERIFIED` applied in order with `ON_ERROR_STOP` on
  PostgreSQL 16.13 on 2026-08-18; after application, zero `schema.prisma`
  models lack tables (previously 10 S4NP Phase 3/4/5 models had none).
  `scripts/validate-migrations.js` (additive-only, SPEC-P0 R8) passes on all
  21 files.
- Integration suites: 13 database suites (5 in `packages/db`, 8 in
  `packages/org-autonomous-ops-context`). In CI they are **required**:
  `REQUIRE_DB_TESTS=1` turns any database-unavailable skip into a failing
  test (`dbTestGuard.ts`), and `assertSafeTestDatabaseUrl` refuses
  non-loopback or managed-provider hosts before any connection (SPEC-P0
  R2/R3).
- Local counts (2026-08-18, no database engine available in the authoring
  sandbox): `packages/db` 17 passing / 5 suites skipped-without-DB;
  `packages/org-autonomous-ops-context` 77 passing / 6 suites
  skipped-without-DB. CI counts with the ephemeral database run higher and
  are visible in the "Run tests" step of the CI run; skips cannot pass
  there. Test-count floors are enforced per package by
  `scripts/assert-test-count.js` (auth ≥ 2, db ≥ 17, agents ≥ 51,
  billing ≥ 1, worker-financial-layer ≥ 2).

## Rollback owner

- **Owner: `@chinyeosemene`** — assigned by the product owner in the PR #11
  merge approval (2026-08-18). Rollback path: redeploy the previous
  known-good ref; all migrations are additive-only (SPEC-P0 R8, enforced by
  `scripts/validate-migrations.js`), so the prior application version runs
  against the migrated schema without a schema rollback.

## Approved scope

This branch remediates the SPEC-P0 blockers, and nothing else:

| Item | Scope | State |
| --- | --- | --- |
| P0-6 | `apps/web` middleware moved to `src/`; build-artifact manifest test | Done on branch |
| P0-1 | Deterministic test discovery + per-package count floors (`scripts/assert-test-count.js`) | Done on branch |
| P0-3 | CI PostgreSQL 16 service; migrations applied; DB suites required; production-DB guard | Done on branch |
| P0-4 | `WorkerService`/HERALD: null + provenance for unavailable data; deterministic content-hash opportunity ids; unidentifiable records rejected | Done on branch |
| P0-5 | This release record; blockers doc archived with pointer | Done on branch |
| P0-2 | Stripe key handling | **DEFERRED** — no Stripe key, route, or billing change on this branch |

Explicitly out of scope (unchanged from the archived blockers document):
production launch approval, mobile availability, worker financial layer
activation, grant-generator externalization, and any agent autonomy
expansion (SPEC-P0 R9 — the `internal_only`/`ask_first`/`never` matrix is
untouched).

## Remaining stop-ship items

The archived blockers document remains the inventory of pre-existing
stop-ship findings not covered by SPEC-P0 (auth architecture, Redis-backed
rate limiting, MCP transport mounting, worker financial placeholders).
Those stand until each gains its own evidence trail. See
[`docs/releases/archive/BLOCKERS_TO_PRODUCTION-2026-06-11.md`](archive/BLOCKERS_TO_PRODUCTION-2026-06-11.md)
and [`AUDIT_REPORT.md`](../../AUDIT_REPORT.md) (2026-04-12 baseline).
